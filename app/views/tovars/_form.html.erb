<%= form_with(model: tovar, local: true, :html => {class: 'form-horizontal'}) do |form| %>
  <% if tovar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tovar.errors.count, "error") %> prohibited this tovar from being saved:</h2>

      <ul>
      <% tovar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Наименование:", :class => "col-sm-3 control-label" %>
    <div class="col-sm-9">
      <%= form.text_field :name, :class => "form-control"  %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-3 col-sm-9">
      <%= form.submit "Сохранить", :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
